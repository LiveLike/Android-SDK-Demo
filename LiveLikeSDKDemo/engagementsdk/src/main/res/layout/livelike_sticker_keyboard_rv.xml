<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/list_item"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/empty_recent_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:gravity="center_horizontal|center_vertical|center"
        android:text="@string/livelike_chat_recent_stickers_empty_message"
        android:textColor="@color/livelike_sticker_keyboard_empty_text_color"
        android:textSize="@dimen/livelike_sticker_keyboard_empty_text_size"
        android:visibility="gone"
        tools:text="@string/livelike_chat_recent_stickers_empty_message" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvStickers"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />


</RelativeLayout>